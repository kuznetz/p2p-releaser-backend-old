{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start:watch",
            "request": "launch",
            "command": "npm run start:watch",
            "cwd": "${workspaceFolder}"
        }
    ]
}